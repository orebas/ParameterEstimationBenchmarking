### This file is machine-generated.
### Date: 2025-09-17 07:04:19
### Author: ad7760
### Generated by: src/generate_data.py -d results/september_16_2025_search_bound_100 config/config.json config/systems.json
### Environment: /scratch/ad7760/no-matlab-no-worry/environments/venv

using Pkg; Pkg.activate(joinpath(dirname(dirname(dirname(dirname(@__DIR__)))), string(Symbol(:environments, /, :julia_odepe))))

using MKL

using ModelingToolkit, DifferentialEquations
using ODEParameterEstimation, Distributions
using OrderedCollections

solver = Vern9()


const t = ModelingToolkit.t_nounits
const D = ModelingToolkit.D_nounits

parameters = @parameters g a b
states = @variables VV(t) R(t) 
observables = @variables y1(t)
p_true = [0.32, 0.574, 0.817]
ic = [0.425, 0.542]

equations =             [
                             D(VV) ~ g * (VV - VV^3 / 3 + R),
                             D(R) ~ 1 / g * (VV - a + b * R),
                        ]


measured_quantities = [
        y1 ~ VV,
]

model, mq = create_ordered_ode_system("fitzhugh_nagumo", states, parameters, equations, measured_quantities)

PEP = ParameterEstimationProblem(
    "fitzhugh_nagumo",
    model,
    mq,
    nothing,
    [-1.0, 1.0],
    nothing,
    OrderedDict(parameters .=> p_true),
    OrderedDict(states .=> ic),
    0,
)


opts = EstimationOptions(
    datasize=1001,
    time_interval=[-1.0, 1.0]
)
estimation_problem_with_data = sample_problem_data(PEP, opts)

data_sample = estimation_problem_with_data.data_sample

n = Normal(0.0, 1e-8)
for (key, value) in data_sample
	if key == "t"
		continue
	end
	# data_sample[key] = data_sample[key] .* (1. .+ rand(n, length(data_sample[key])))
end

ks = data_sample.keys
dat_str = ""
for i=1:1001
  global dat_str = dat_str * string(data_sample["t"][i]) * ", " * join(collect(data_sample[ks[j]][i] for j=1:(length(ks)-1)), ", ") * "\n"
end
write(joinpath(@__DIR__, "../data_original/fitzhugh_nagumo_4.csv"), dat_str)

