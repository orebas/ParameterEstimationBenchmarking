#!/bin/bash
# GCC wrapper for MATLAB MEX compilation.
# Suppresses GCC 14 strict errors that break AMIGO2's legacy C code.
# This wrapper is NOT a patch to AMIGO2 — it's a build environment shim.
exec /usr/bin/gcc -Wno-error=incompatible-pointer-types -Wno-error=implicit-function-declaration "$@"
